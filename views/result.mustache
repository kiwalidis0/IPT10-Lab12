<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Result</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-white">
    <div class="container mt-5">
        <h2 class="text-center">Exam Results</h2>
        <h3 class="text-center">Hello, {{ complete_name }} &lt;{{ email }}&gt;</h3>

        <h1 class="text-center">Your Score: {{ total_score }} / {{ question_items }}</h1>

        <table class="table table-dark mt-4">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Question</th>
                    <th>Choices</th>
                    <th>Your Answer</th>
                    <th>Correct Answer</th>
                </tr>
            </thead>
            <tbody>
                {{#questions}}
                <tr>
                    <td>{{ item_number }}</td>
                    <td>{{ question }}</td>
                    <td>
                        <ul class="list-unstyled">
                            {{#choices}}
                            <li><strong class="text-primary">{{ letter }}</strong> {{ choice }}</li>
                            {{/choices}}
                        </ul>
                    </td>
                    <td><strong>{{ user_answer }}</strong></td>
                    <td><strong class="text-success">{{ correct_answer }}</strong></td>
                </tr>
                {{/questions}}
            </tbody>
        </table>

        <div class="text-center mt-4">
            <a href="/login" class="btn btn-primary">Restart Exam</a>
            <a href="/examinees" class="btn btn-success">View All Results</a>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
